# Doc125 V3 Doc117–124 Mainline Integration Audit Checkpoint

Status: reviewed integration checkpoint. This document records the isolated
mainline audit of the cumulative shared-foundation candidate
`origin/codex/doc118-review-projection-closure-20260715@8724a3d`. It is not a
production-enable decision and does not replace any real Provider acceptance
gate.

## 1. Audited scope

The candidate contains Doc117 and Docs118–124 plus the corresponding shared
runtime changes. Its responsibility is limited to the following boundaries:

```text
frozen capability evidence
→ shared Human Realism activation
→ Provider reference/result provenance
→ shared real-pixel review and bounded retry
→ safe public delivery projection
```

It does not add a children, apparel, E-Commerce, Photography, or General
Template-specific capability, Provider route, static image recipe, local text
renderer, OCR loop, or structured prompt stack.

## 2. Verified invariants

1. Explicit visible-young-person wording becomes ordinary shared `person` and
   `visible_person` evidence; a product-only children's-garment flat lay does
   not do so in the enforced activation plan.
2. Human Realism remains the only owner of the safety-sensitive auxiliary
   signal. Provider materialization removes only framework-owned anatomy,
   beauty, micro-detail, and raw review-code wording; it does not rewrite user
   intent, remote-Brain direction, reference-channel ownership, or garment
   facts.
3. Explicit Provider policy signals are terminal `provider_policy_blocked`
   results. They create no outer V3 retry, pixel review, retry candidate, or
   final delivery.
4. Public Job and Project surfaces distinguish review mode, verification, and
   automatic-delivery availability without exposing raw Provider diagnostics
   or retry material.
5. The candidate preserves General, E-Commerce, and Photography ownership. It
   adds no vertical deliverable roles or creative direction to the shared
   foundation.

## 3. Isolated verification

In `D:\AI\w\doc124int-20260715`:

```text
python -m pytest alchemy_creative_agent_3_0/tests -q
736 passed in 26.65s

python -m compileall -q alchemy_creative_agent_3_0/app alchemy_creative_agent_3_0/tests
node --check src_skeleton/app/static/app.js
git diff --check
```

All static checks exited successfully.

## 4. Controlled General evidence classification

The originating branch reports a controlled General task
`job_a5a6e36dac` with an authorized blue-dress source, remote Brain used with
no fallback, GPT Image 2 pixels, one shared bounded retry, and a final
`hybrid / pass / verified` result. The durable runtime record is not stored in
this integration worktree, so this checkpoint classifies it as reviewed branch
evidence, not independently replayed evidence.

If its provenance is made available, it may support the narrow statement:

> One controlled General declared-garment/visible-young-person regression has
> a real-pixel shared-review pass.

It must not be generalized to SKU-perfect garment truth, multi-output
diversity, E-Commerce Gate C/D, Photography P10, Browser Gate D, or any
production-enable claim.

## 5. Integration order and document numbering

1. Merge this cumulative candidate only from a clean main worktree, after a
   final rebase and regression run.
2. The candidate owns Docs118–124. Preserve those numbers and their internal
   cross-references as a coherent correction series.
3. The separate Codex Native ImageGen N1 candidate currently uses an unrelated
   Doc118 number. Before it is integrated, rename that N1 document to the next
   free mainline number after this checkpoint and update its code, test, and
   plugin references. Never retain two authoritative Doc118 documents.
4. Keep all production gates closed until their independent, rights-clear,
   real-Provider and real-pixel acceptance matrices are complete.

## 6. Non-goals

- No prompt escalation or policy bypass for Provider-blocked requests.
- No children- or kidswear-specific shared route.
- No local typography, OCR, canvas, safe-area, slot recipe, or deterministic
  image composition path.
- No modification to specialized-template deliverable ownership.
